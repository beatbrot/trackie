coverage:
  status:
    project:
      default:
        target: 50%    # the required coverage value
        threshold: 1%  # the leniency in hitting the target

ignore:
  - benches/*
  - tests/*
  # These are tested via the integration tests
  - src/main.rs
  - src/persistence.rs
